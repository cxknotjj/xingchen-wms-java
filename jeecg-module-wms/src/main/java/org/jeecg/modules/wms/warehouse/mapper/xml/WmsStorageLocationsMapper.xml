<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.wms.warehouse.mapper.WmsStorageLocationsMapper">

    <select id="queryList" parameterType="org.jeecg.modules.wms.warehouse.entity.WmsStorageLocations" resultType="org.jeecg.modules.wms.warehouse.entity.WmsStorageLocations">
        select a.* , b.warehouse_name , c.zone_name from wms_warehouses b , wms_storage_locations a , wms_storage_zones c where b.id = a.warehouse_id and c.id = a.zone_id
        -- 仓库名称查询
    <if test="warehouseName != null and warehouseName != ''">
        and a.warehouse_name like '%' '%' #{warehouseName} '%'
    </if>
    -- 仓库ID查询
    <if test="warehouseId != null and warehouseId != ''">
        and a.warehouse_id = #{warehouseId}
    </if>
    </select>
</mapper>